services:
  app:
    build:
      context: .
    container_name: vocal-metrics-app
    restart: unless-stopped
    expose:
      - "8501"

  nginx:
    image: nginx:stable-alpine
    container_name: vocal-metrics-nginx
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
